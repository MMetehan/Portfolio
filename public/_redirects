# Netlify _redirects file for SPA routing
# This ensures all routes are handled by React Router

# --- Domain redirect (force non-www) ---
# Only redirect the root domain to avoid conflicts with SPA routing
https://www.metehan-yildirim.com/  https://metehan-yildirim.com/  301!

# Serve the app for all non-file requests (SPA routing)
/*    /index.html   200

# Optional: Redirect specific error pages
/404   /index.html   200

# Optional: Handle old URLs if you had any
# /old-path   /new-path   301
