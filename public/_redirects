# Netlify _redirects file for SPA routing
# This ensures all routes are handled by React Router

# Serve the app for all non-file requests
/*    /index.html   200

# Optional: Redirect specific error pages
/404   /index.html   200

# Optional: Handle old URLs if you had any
# /old-path   /new-path   301
